(function (ng) {
    'use strict';

    ng.module('editor.components.windowPreview')
        .controller('WindowPreviewCtrl', WindowPreviewCtrl);

    WindowPreviewCtrl.$inject = ['$scope', '$location', '$rootScope'];

    function WindowPreviewCtrl($scope, $location, $rootScope) {
        
    }

    WindowPreviewCtrl.prototype = {
       
    };

})(angular);
